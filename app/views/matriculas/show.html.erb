<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Detalhes da Matrícula" %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #275BA8;
      color: #ffffff;
      font-family: 'Inter', sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .details-container {
      background-color: #ffffff;
      color: #333333;
      border-radius: 12px;
      padding: 30px;
      box-shadow: 0px 4px 6px rgba(0,0,0,0.1);
      width: 50%;
      text-align: center;
    }

    h1 {
      font-weight: bold;
      color: #275BA8;
      font-size: 2.5rem;
    }

    p {
      font-size: 1.2rem;
    }

    img {
      max-width: 200px;
      border-radius: 8px;
      margin-top: 15px;
    }

    .btn-secondary {
      background-color: #6c757d;
      color: #ffffff;
      font-weight: bold;
      border-radius: 8px;
      padding: 10px 20px;
    }
  </style>
</head>

<body>
  <div class="details-container">
    <h1>Detalhes da Matrícula</h1>
    <p><strong>Aluno:</strong> <%= @matricula.aluno.nome %></p>
    <p><strong>Disciplina:</strong> <%= @matricula.disciplina.nome %></p>

    <!-- Exibição da Foto -->
    <% if @matricula.foto.attached? %>
  <p><strong>Foto:</strong></p>
  <%= image_tag @matricula.foto, size: "200x200", class: "img-thumbnail" %>
<% else %>
  <p>Nenhuma foto foi enviada.</p>
<% end %>



    <!-- Botões -->
    <div class="d-flex justify-content-center gap-3 mt-4">
      <%= link_to "Voltar", matriculas_path, class: "btn btn-secondary" %>
      <%= link_to "Editar", edit_matricula_path(@matricula), class: "btn btn-warning" %>
      <%= link_to "Excluir", matricula_path(@matricula), method: :delete, 
                    data: { confirm: "Tem certeza que deseja excluir?" }, class: "btn btn-danger" %>
    </div>
  </div>
</body>
</html>
