<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for(:title) || "Controle Acadêmico" %></title>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>

  <!-- Google Fonts - Inter -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">

  <!-- Estilos Personalizados -->
  <style>
    body {
      background-color: #1E3962;
      color: #ffffff;
      font-family: 'Inter', sans-serif;
      text-align: center;
      margin: 0;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    h1 {
      font-weight: 600;
      font-size: 2.5rem;
      margin-bottom: 20px;
    }

    p {
      margin-bottom: 40px;
      font-size: 1.2rem;
    }

    .card-container {
      display: flex;
      justify-content: center;
      gap: 30px;
    }

    .card-link {
      text-decoration: none;
      color: inherit;
    }

    .card {
      background-color: #ffffff;
      border-radius: 15px;
      padding: 20px;
      width: 160px;
      height: 180px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      transition: transform 0.3s, box-shadow 0.3s;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    .card img {
      width: 70px;
      margin-bottom: 10px;
    }

    .card p {
      margin: 0;
      font-weight: 600;
      color: #2C3E50;
    }

    .card:hover {
      transform: translateY(-8px);
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
    }

    .btn-export {
        background-color: #ffffff; /* Cor verde mais suave */
        color: #2C3E50;
        padding: 8px 20px; /* Ajustando o padding para diminuir o tamanho */
        font-size: 0.9rem; /* Tamanho menor da fonte */
        font-weight: 600;
        border-radius: 8px;
        text-transform: uppercase;
        margin-top: 30px;
        transition: background-color 0.3s, transform 0.2s;
      }
    
      .btn-export:hover {
        background-color: #ffffff; /* Cor verde escura ao passar o mouse */
        color: #2C3E50;
        transform: scale(1.02); /* Pequeno efeito ao passar o mouse */
      }
  </style>
</head>
<body>
  <h1>Bem-vindo ao SCA!</h1>
  <p>Escolha uma das opções abaixo para navegar:</p>

  <!-- Cartões clicáveis com links -->
  <div class="card-container">
    <!-- Gerenciar Alunos -->
    <%= link_to alunos_path, class: "card-link" do %>
      <div class="card">
        <%= image_tag 'icones/alunos.png', alt: "Gerenciar Alunos" %>
        <p>GERENCIAR ALUNOS</p>
      </div>
    <% end %>

    <!-- Gerenciar Disciplinas -->
    <%= link_to disciplinas_path, class: "card-link" do %>
      <div class="card">
        <%= image_tag 'icones/disciplinas.png', alt: "Gerenciar Disciplinas" %>
        <p>GERENCIAR DISCIPLINAS</p>
      </div>
    <% end %>

    <!-- Gerenciar Matrículas -->
    <%= link_to matriculas_path, class: "card-link" do %>
      <div class="card">
        <%= image_tag 'icones/matriculas.png', alt: "Gerenciar Matrículas" %>
        <p>GERENCIAR MATRÍCULAS</p>
      </div>
    <% end %>
  </div>

  <!-- Botão Exportar Dados -->
  <%= link_to "EXPORTAR TODOS OS DADOS (EM CSV)", exportar_csv_path, class: "btn btn-export" %>
</body>
</html>
