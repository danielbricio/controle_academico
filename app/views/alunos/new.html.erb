<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Novo Aluno" %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #1E3962;
      color: #ffffff;
      font-family: 'Inter', sans-serif;
    }

    .header-icon {
      background-color: #ffffff;
      border-radius: 12px;
      width: 80px;
      height: 80px;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0 auto 20px;
    }

    .form-container {
      background-color: #ffffff;
      border-radius: 12px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      padding: 30px;
      width: 60%;
      margin: 0 auto;
      color: #333333;
    }

    .form-label {
      font-weight: bold;
      color: #275BA8;
    }

    .btn-submit {
      background-color: #ffffff;
      color: #275BA8;
      border-radius: 8px;
      padding: 10px 20px;
      font-weight: bold;
      text-transform: uppercase;
      border: 1px solid #275BA8;
    }

    .btn-submit:hover {
      background-color: #f1f1f1;
      color: #1D4B8C;
    }

    .btn-back {
      background-color: #275BA8;
      color: #ffffff;
      border-radius: 8px;
      padding: 10px 20px;
      font-weight: bold;
      text-transform: uppercase;
      text-decoration: none;
      border: 1px solid #275BA8;
    }

    .btn-back:hover {
      background-color: #1D4B8C;
      color: #ffffff;
    }
  </style>
</head>
<body>
  <div class="container text-center mt-5">
    <!-- Ícone e título -->
    <div class="header-icon">
      <%= image_tag 'icones/alunos.png', alt: "Ícone Aluno", style: "width: 50px; height: 50px;" %>
    </div>
    <h1>Cadastrar Novo Aluno</h1>
    
    <!-- Formulário -->
    <div class="form-container">
      <%= form_with model: @aluno, local: true do |form| %>
        <div class="mb-3">
          <%= form.label :nome, "Nome", class: "form-label" %>
          <%= form.text_field :nome, class: "form-control", placeholder: "Digite o nome do aluno" %>
        </div>
        
        <div class="mb-3">
          <%= form.label :idade, "Idade", class: "form-label" %>
          <%= form.number_field :idade, class: "form-control", placeholder: "Digite a idade" %>
        </div>
        
        <div class="mb-3">
          <%= form.label :email, "Email", class: "form-label" %>
          <%= form.email_field :email, class: "form-control", placeholder: "Digite o email" %>
        </div>

        <!-- Botões lado a lado -->
        <div class="d-flex justify-content-between mt-4">
          <%= form.submit "Salvar Aluno", class: "btn btn-submit" %>
          <%= link_to "Voltar", alunos_path, class: "btn btn-back" %>
        </div>
      <% end %>
    </div>
  </div>
</body>
</html>
