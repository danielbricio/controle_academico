<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Detalhes do Aluno" %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #275BA8;
      color: #ffffff;
      font-family: 'Inter', sans-serif;
    }

    .details-container {
      background-color: #ffffff;
      color: #333333;
      border-radius: 12px;
      padding: 30px;
      width: 50%;
      margin: 50px auto;
      box-shadow: 0px 4px 6px rgba(0,0,0,0.1);
      text-align: center;
    }

    h1 {
      font-weight: bold;
      color: #275BA8;
    }

    .btn-back {
      background-color: #275BA8;
      color: #ffffff;
      border: none;
      border-radius: 8px;
      padding: 10px 20px;
      text-decoration: none;
      font-weight: bold;
    }

    .btn-back:hover {
      background-color: #1D4B8C;
      color: #ffffff;
    }
  </style>
</head>
<body>
  <div class="details-container">
    <h1><%= @aluno.nome %></h1>
    <p><strong>Idade:</strong> <%= @aluno.idade %></p>
    <p><strong>Email:</strong> <%= @aluno.email %></p>

    <!-- Botão Voltar -->
    <%= link_to "Voltar à Lista de Alunos", alunos_path, class: "btn btn-back mt-3" %>
  </div>
</body>
</html>
