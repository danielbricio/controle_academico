<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Detalhes da Disciplina" %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #275BA8;
      color: #ffffff;
      font-family: 'Inter', sans-serif;
    }

    .header-icon {
      background-color: #ffffff;
      border-radius: 12px;
      width: 80px;
      height: 80px;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0 auto 20px;
    }

    .details-container {
      background-color: #ffffff;
      color: #333333;
      border-radius: 12px;
      box-shadow: 0px 4px 6px rgba(0,0,0,0.1);
      padding: 30px;
      width: 50%;
      margin: 30px auto;
      text-align: center;
    }

    h1 {
      font-weight: bold;
      color: #ffffff;
      margin-bottom: 20px;
    }

    .details-container p {
      font-size: 18px;
      margin-bottom: 10px;
    }

    .btn-custom {
      background-color: #275BA8;
      color: #ffffff;
      border-radius: 8px;
      padding: 8px 15px;
      font-weight: bold;
      text-decoration: none;
    }

    .btn-custom:hover {
      background-color: #1D4B8C;
      color: #ffffff;
    }

    .btn-warning {
      font-weight: bold;
    }
  </style>
</head>

<body>
  <div class="container text-center mt-5">
    <!-- Ícone e título -->
    <div class="header-icon">
      <%= image_tag 'icones/disciplinas.png', alt: "Ícone Disciplina", style: "width: 50px; height: 50px;" %>
    </div>
    <h1>Detalhes da Disciplina</h1>

    <!-- Detalhes da Disciplina -->
    <div class="details-container">
      <p><strong>Nome:</strong> <%= @disciplina.nome %></p>
      <p><strong>Código:</strong> <%= @disciplina.codigo %></p>

      <!-- Botões -->
      <div class="d-flex justify-content-center gap-3 mt-3">
        <%= link_to "Editar", edit_disciplina_path(@disciplina), class: "btn btn-warning btn-sm" %>
        <%= link_to "Voltar", disciplinas_path, class: "btn btn-custom btn-sm" %>
      </div>
    </div>
  </div>
</body>
</html>
