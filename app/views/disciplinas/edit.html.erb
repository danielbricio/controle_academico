<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Editar Disciplina" %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #275BA8;
      color: #ffffff;
      font-family: 'Inter', sans-serif;
      height: 100vh;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .form-container {
      background-color: #ffffff;
      border-radius: 12px;
      padding: 30px;
      box-shadow: 0px 4px 6px rgba(0,0,0,0.1);
      width: 50%;
      text-align: center;
    }

    h1 {
      font-weight: bold;
      color: #275BA8;
      font-size: 2.5rem;
    }

    .subtitle {
      font-size: 1.2rem;
      font-weight: bold;
      color: #275BA8;
      margin-bottom: 20px;
    }

    .btn-primary {
      background-color: #275BA8;
      color: #ffffff;
      font-weight: bold;
      border: none;
      padding: 8px 15px;
      border-radius: 8px;
    }

    .btn-primary:hover {
      background-color: #1D4B8C;
      color: #ffffff;
    }

    .btn-secondary {
      background-color: #6c757d;
      color: #ffffff;
      font-weight: bold;
      border: none;
      padding: 8px 15px;
      border-radius: 8px;
    }

    .form-control {
      border-radius: 8px;
      border: 1px solid #ccc;
      padding: 10px;
    }

    label {
      font-weight: bold;
      color: #333333;
      margin-bottom: 5px;
    }
  </style>
</head>

<body>
  <div class="form-container">
    <h1>Editar Disciplina</h1>
    <p class="subtitle">Atualize o cadastro da disciplina preenchendo os campos abaixo</p>

    <%= form_with(model: @disciplina, local: true) do |form| %>
      <% if @disciplina.errors.any? %>
        <div class="alert alert-danger text-start">
          <h2><%= pluralize(@disciplina.errors.count, "erro") %> encontrados:</h2>
          <ul>
            <% @disciplina.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <!-- Nome -->
      <div class="mb-3 text-start">
        <%= form.label :nome %>
        <%= form.text_field :nome, class: "form-control" %>
      </div>

      <!-- Código -->
      <div class="mb-3 text-start">
        <%= form.label :codigo %>
        <%= form.text_field :codigo, class: "form-control" %>
      </div>

      <!-- Botões -->
      <div class="d-flex justify-content-center gap-3 mt-4">
        <%= form.submit "Atualizar Disciplina", class: "btn btn-primary" %>
        <%= link_to "Voltar", disciplinas_path, class: "btn btn-secondary" %>
      </div>
    <% end %>
  </div>
</body>
</html>
